import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Setup basic global styles or resets if needed beyond main.css
ReactDOM.createRoot(document.getElementById('root')!).render(
    <App />
);
