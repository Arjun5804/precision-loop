{
  "name": "@precision-loop/playback-engine",
  "version": "0.1.0",
  "description": "Playback Engine for Precision Loop",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@precision-loop/audio-engine": "*",
    "@precision-loop/audio-scheduler": "*",
    "@precision-loop/loop-model": "*"
  },
  "devDependencies": {
    "vitest": "^1.2.2",
    "typescript": "^5.3.3"
  }
}
