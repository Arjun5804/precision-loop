{
  "name": "precision-loop",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "fast-check": "^3.16.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
